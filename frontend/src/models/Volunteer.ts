export default interface volunteerModel {
  skills: { name: string }[];
  role: string;
  description: string;
  content: string;
  contact_email: string;
  contact_phone: string;
  image: string;
  id: number;
}
